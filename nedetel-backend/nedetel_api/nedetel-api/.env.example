# PostgreSQL connection string for Prisma
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/nedetel_db?schema=public"

# Optional performance cache TTL in milliseconds
PERF_CACHE_TTL_MS=45000

# Optional Redis URL for BullMQ queue (if missing, in-memory fallback is used)
REDIS_URL="redis://localhost:6379"
